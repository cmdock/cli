# Implementation Notes

This directory holds temporary implementation notes, research writeups, and
working design documents when `cmdock-admin` grows beyond ADR-scale decisions.

The repo does not need implementation notes for every feature. Add files here
only when a workstream needs active design or migration tracking.
